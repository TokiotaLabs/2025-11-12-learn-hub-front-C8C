## Use Selenium WebDriver to test the Home component.
Write a test to verify that the title (h1) text matches the expected value "Unlock Your Potential".
Set up the Selenium WebDriver to navigate to the Home page and locate the h1 element.
Assert that the h1 element's text matches the expected title.


## New test get first element <p> inside Home.tsx to locate an element by its class name using Selenium's findElement method, you can use the className locator. Here's how you can modify the test to find the <p> element by its class:


## Can you write a Selenium test that inside test folder:
*Navigates to http://localhost:5173/new
*Fill all the inputs required inside courseForm
*Click the submit button
*Asset the createCourse is called